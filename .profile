# ~/.profile: executed by login shells.

# This file is not read by bash, if ~/.bash_profile
# or ~/.bash_login exist, but we will source it from ~/.bash_profile.

export PATH=$HOME/.local/bin:$HOME/bin:$PATH
